<% if current_user.following?(@image.user) || current_user == @image.user%>
<div id="like_form">
    <%if @image.liked_by?(current_user) %>
        <%= render 'unlike' %>
    <%else %>
        <%= render 'like',  image: @image %>
    <% end %>
</div>
<%end%>